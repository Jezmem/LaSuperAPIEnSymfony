<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sorties jeux vidéo de la semaine</title>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
</head>
<body class="bg-light py-5">
    <div class="container">
        <h1 class="text-center text-primary mb-4">Les sorties jeux vidéo des 7 prochains jours</h1>

        {% if games is empty %}
            <p class="text-center">Aucun jeu prévu cette semaine.</p>
        {% else %}
            <ul class="list-group">
                {% for game in games %}
                    <li class="list-group-item mb-3 shadow-sm">
                        <h4 class="font-weight-bold">{{ game.title }}</h4>
                        <p class="text-muted">Sortie prévue le <strong>{{ game.releaseDate|date('d/m/Y') }}</strong></p>
                        <p><em>Éditeur :</em> {{ game.editor.name }}</p>
                        <p><em>Catégorie :</em> {{ game.category.name }}</p>
                        <p>{{ game.description }}</p>
                    </li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
</body>
</html>
